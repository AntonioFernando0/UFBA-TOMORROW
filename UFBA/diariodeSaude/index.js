// declaração das variáveis para obter as datas
const dataInput = document.getElementById("dataInput");
const dataAtual = new Date().toISOString().split('T')[0];
dataInput.value = dataAtual;

// declare a variável relacionada ao botão registrar


// declare a variável que terá a lista de registros



// crie uma função para registrar os dados
function registrarDado() {
    const data = dataInput.value;
    // declare as variáveis para obter a hora, o tipo de registro e o valor


    // declare a variável que terá a tabela de registros

    
    // declare a variável para inserir a linha na tabela de registros (inserRow(1))

    // declare as variáveis que irão incluir as células (colunas / dados) na tabela
    // lembre-se que a primeira célula tem o índice 0
    // as celulas estarão sem conteúdo

    
    // altere o conteúdo das células



    // Limpe os campos do formulário após o registro

}

// inclua o eventlistener para registrar o dado